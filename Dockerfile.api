# API Dockerfile
FROM node:20-alpine
WORKDIR /app

# Install dependencies
COPY api-server/package*.json ./
RUN npm ci --silent

# Copy source
COPY api-server/ .

ENV NODE_ENV=production
EXPOSE 3000
CMD ["node", "index.js"]
